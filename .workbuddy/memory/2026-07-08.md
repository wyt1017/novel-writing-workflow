# 2026-07-08 工作流实测运行 + 隐患审计

## 今日动作
- 按流畅模式 v2.1 真实跑了一遍写作路径：读 `设定速查卡.md` + 进度追踪 → 写 `第4章.md`（约2380字，钩子"系统任务帮同事搬家→碰巧解决邻里纠纷"）
- 尝试调用 `novel-de-ai` 做章检 → **报错：Can not find skill**

## 关键发现（隐患）
1. **[致命] Skill 路径错误**：`novel-writing`、`novel-de-ai` 放在 `.trae/skills/`（TRAE 目录），WorkBuddy 的 Skill 工具只扫 `.workbuddy/skills/` 与 `~/.workbuddy/skills/`。后果：①入口 Skill 触发条件永不自动激活；②"每章调用 novel-de-ai"运行时直接失败。之前的"成功运行"其实是 agent 手动执行，从未真正调起 Skill。
   - 修复方案：把两 Skill 迁移到 `.workbuddy/skills/`（待用户确认后执行）
2. **[中] 大文档节点体内仍写"停止并等待确认"**：v2.1 只改了头部规则4和统一说明，但 2000 行文档内各节点 body 仍保留"每节点强确认"措辞，AI 回读大文档会倒退回旧卡顿模式
3. **[中] 速查卡漂移风险**：SKILL 要求"改原文件+同步更新速查卡"但无强制校验，长篇小说必漂移
4. **[低] 速查卡内部矛盾**：写"第10章才解锁精神震慑"，但 ch3 已作为奖励发放（限制是"本周禁用"所以一致，但措辞易误导）
5. **[低] 产出未提交即丢的复发风险**：本次新写第4章先本地 commit 防丢（之前误删过）
6. **[低] 章检双标准**：de-ai 评分/50，工作流章检表/100，口径不一

## 已处理
- 更新 `输出/进度追踪.md`：标记第4章完成、下一步写第5章、备注栏写入 Skill 路径已知问题
- 本地 commit（未推送，网络代理此前异常）

## 待办（需用户确认）
- 是否将 `.trae/skills/{novel-writing,novel-de-ai}` 迁移至 `.workbuddy/skills/` 彻底修复致命隐患
